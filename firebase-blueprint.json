{
  "entities": {
    "LearningSession": {
      "title": "Learning Session",
      "description": "A record of a single learning session with cognitive metrics",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "durationMinutes": { "type": "number" },
        "subject": { "type": "string" },
        "perceivedComplexity": { "type": "number" },
        "distractions": { "type": "number" },
        "energyLevel": { "type": "number" },
        "performanceScore": { "type": "number" },
        "sessionType": { "type": "string" },
        "studyEnvironment": { "type": "string", "enum": ["Quiet", "Moderate Noise", "Distracting"] },
        "flowStateRating": { "type": "number" },
        "notes": { "type": "string" },
        "cognitiveDebitScore": { "type": "number" }
      },
      "required": ["id", "userId", "timestamp", "durationMinutes", "subject", "perceivedComplexity", "distractions", "energyLevel", "performanceScore", "cognitiveDebitScore", "sessionType", "studyEnvironment", "flowStateRating"]
    },
    "LearningPreferences": {
      "title": "Learning Preferences",
      "description": "User specific learning parameters",
      "type": "object",
      "properties": {
        "primaryGoal": { "type": "string" },
        "preferredSessionDuration": { "type": "number" },
        "dailyTargetMinutes": { "type": "number" },
        "learningStyle": { "type": "string" },
        "focusMusic": { "type": "boolean" }
      }
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Information about the user and their cognitive profile",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "bio": { "type": "string" },
        "joinedAt": { "type": "string", "format": "date-time" },
        "lastAnalysisInsights": { "type": "string" },
        "lastDebitScore": { "type": "number" },
        "hasSeenTutorial": { "type": "boolean" },
        "preferences": { "$ref": "#/entities/LearningPreferences" }
      },
      "required": ["uid", "email", "joinedAt", "preferences"]
    },
    "LearningReminder": {
      "title": "Learning Reminder",
      "description": "A scheduled reminder for a future learning session",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "subject": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "sessionType": { "type": "string" },
        "notified": { "type": "boolean" }
      },
      "required": ["id", "userId", "subject", "timestamp", "sessionType", "notified"]
    }
  },
  "firestore": {
    "/profiles/{uid}": {
      "schema": "UserProfile",
      "description": "User profiles and aggregate stats"
    },
    "/sessions/{sessionId}": {
      "schema": "LearningSession",
      "description": "Individual learning sessions indexed for retrieval"
    },
    "/reminders/{reminderId}": {
      "schema": "LearningReminder",
      "description": "Scheduled notifications for future work"
    }
  }
}
